# Paranoia Agent Memory

Project-scoped memory shared by agents declared with `memory: project` in their frontmatter.

Per-agent indexes live in `agent-memory/<agent>/MEMORY.md`.

## Seeded notes

- [Modifier never blocks a turn](feedback_modifier_fallback.md) — failures must fall through with `was_modified=false`.
